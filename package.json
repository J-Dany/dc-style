{
  "name": "dc-style",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "sass src/style.scss style.css --load-path=node_modules --watch",
    "compile": "sass src/style.scss style.css --load-path=node_modules",
    "clean": "cleancss -O2 style.css -o style.css",
    "dev:dashboard": "sass src/dashboard.scss dashboard.css --load-path=node_modules --watch",
    "compile:dashboard": "sass src/style.scss style.css --load-path=node_modules",
    "clean:dashboard": "cleancss -O2 style.css -o style.css"
  },
  "devDependencies": {
    "bootstrap": "^5.3.2",
    "bootstrap-icons": "^1.11.3",
    "clean-css": "^5.3.3",
    "clean-css-cli": "^5.6.3",
    "normalize.css": "^8.0.1",
    "sass": "^1.70.0"
  }
}
